# PolyMOCK

PolyMOCK — это полнофункциональное приложение рынка предсказаний, разработанное как клон/имитация Polymarket.

## 🚀 Возможности

- **Рынки предсказаний**: Просмотр и торговля на различных рынках.
- **Аутентификация пользователей**: Безопасная регистрация и вход с использованием JWT.
- **Кошелек и транзакции**: Управление средствами пользователя и позициями на рынке.
- **Панель администратора**: Управление рынками и пользователями.
- **Адаптивный интерфейс**: Создан с использованием Tailwind CSS и Shadcn UI.

## 🛠 Технологический стек

### Бэкенд
- **Фреймворк**: [FastAPI](https://fastapi.tiangolo.com/) (Python)
- **База данных**: PostgreSQL
- **ORM**: SQLAlchemy (Async)
- **Миграции**: Alembic
- **Аутентификация**: PyJWT, Passlib

### Фронтенд
- **Фреймворк**: [React](https://react.dev/)
- **Сборщик**: [Vite](https://vitejs.dev/)
- **Язык**: TypeScript
- **Стилизация**: Tailwind CSS
- **UI Компоненты**: Shadcn UI, Radix UI
- **Управление состоянием**: Zustand, React Query

### Инфраструктура
- **Контейнеризация**: Docker & Docker Compose
- **Администрирование БД**: Adminer

## 🏁 Начало работы

Самый простой способ запустить проект — использовать Docker Compose.

### Предварительные требования
- Docker и Docker Compose, установленные на вашем компьютере.

### Быстрый старт (Docker)

1. **Клонируйте репозиторий:**
   ```bash
   git clone <repository-url>
   cd PolyMOCK
   ```

2. **Настройте переменные окружения:**
   Скопируйте пример файла окружения и обновите его вашими настройками (особенно если у вас есть ключ API OpenRouter).
   ```bash
   cp .env-example .env
   ```

3. **Запустите приложение:**
   ```bash
   docker-compose up -d --build
   ```

   Это запустит следующие сервисы:
   - **Бэкенд**: http://localhost:8000
   - **Фронтенд**: http://localhost:3000
   - **База данных**: PostgreSQL (порт 5432)
   - **Adminer** (UI базы данных): http://localhost:8080

### Локальная разработка

Если вы предпочитаете запускать сервисы по отдельности без Docker:

#### Настройка Бэкенда

1. Перейдите в директорию бэкенда:
   ```bash
   cd backend
   ```

2. Создайте виртуальное окружение и активируйте его:
   ```bash
   python -m venv venv
   source venv/bin/activate  # В Windows: venv\Scripts\activate
   ```

3. Установите зависимости:
   ```bash
   pip install -e .
   # или если используете uv/poetry в зависимости от предпочтений
   ```

4. Запустите миграции:
   Убедитесь, что ваша локальная база данных PostgreSQL запущена и настроена в `.env`.
   ```bash
   alembic upgrade head
   ```

5. Запустите сервер:
   ```bash
   cd src
   uvicorn main:app --reload
   ```

#### Настройка Фронтенда

1. Перейдите в директорию фронтенда:
   ```bash
   cd frontend
   ```

2. Установите зависимости:
   ```bash
   npm install
   ```

3. Запустите сервер разработки:
   ```bash
   npm run dev
   ```
   Фронтенд будет доступен по адресу http://localhost:5173 (порт Vite по умолчанию) или по порту, настроенному в `vite.config.ts`.

## 📂 Структура проекта

```
PolyMOCK/
├── backend/            # Приложение FastAPI
│   ├── alembic/        # Миграции базы данных
│   ├── src/            # Исходный код (API, Модели, Сервисы)
│   └── tests/          # Python тесты
├── frontend/           # Приложение React
│   ├── src/            # Исходный код (Компоненты, Страницы, Хранилища)
│   └── public/         # Статические ресурсы
└── docker-compose.yml  # Оркестрация Docker
```